# Required only for `npm run discover` (the LLM discovery run).
# Replay, catalog, and the operator console never call a model.
ANTHROPIC_API_KEY=sk-ant-...

# Optional overrides
CUA_MODEL=claude-sonnet-5
LEGACY_CORE_PORT=4173
OPERATOR_PORT=4180
