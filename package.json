{
  "name": "computer-use-automation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "LLM discovery -> versioned capability artifact -> deterministic replay -> human handoff, for legacy back-office applications.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "app": "tsx apps/legacy-core/server.ts",
    "discover": "tsx src/cli.ts discover",
    "replay": "tsx src/cli.ts replay",
    "operator": "tsx src/cli.ts operator",
    "catalog": "tsx src/cli.ts catalog",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.124.0",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "playwright": "^1.56.0",
    "yaml": "^2.8.1",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.7.0",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
